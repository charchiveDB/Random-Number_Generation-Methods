rexpzig <- function(n) {
  u <- runif(n)
  x <- u # add code to transform to pdf here
  return (x)
}

rnormzig <- function(n) {
  u <- runif(n)
  x <- u # add code to transform to pdf here
  return (x)
}